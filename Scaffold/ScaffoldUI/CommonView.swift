import SwiftUI

struct CommonView: View {
    var body: some View {
        VStack {
            Text("Hello, World!")
            Image(systemName: "globe")
        }
    }
}

#Preview {
    CommonView()
}
